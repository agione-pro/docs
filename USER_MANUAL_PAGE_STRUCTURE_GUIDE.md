# 用户手册页面类型化写作指南

> 状态：样本验证阶段
> 更新日期：2026-09-07
> 适用范围：`docs/usermanual/` 与 `docs/zh-CN/usermanual/`

## 1. 目标

在不改变用户手册目录、系统菜单映射、页面 URL 和现有信息量的前提下，根据页面承担的任务调整章节重点，减少重复描述，让用户更快判断“页面展示什么、可以做什么、如何确认结果”。

本指南不用于重建用户手册的信息架构，也不要求所有页面使用相同篇幅。

## 2. 固定章节骨架

用户手册继续保留以下十个二级章节，中英文页面应保持对应：

1. 功能概述 / Feature Overview
2. 前提条件 / Prerequisites
3. 页面说明 / Page Description
4. 主要操作 / Main Operations
5. 参数速查表 / Parameter Quick Reference
6. 踩坑提示 / Pitfalls
7. 结果校验 / Result Validation
8. 常见问题 / FAQ
9. 注意事项 / Notes
10. 后续操作 / Next Steps

页面类型只决定各章节的内容重点和篇幅，不新增用户可见目录层级。

## 3. 页面类型判断

| 页面类型 | 判断标准 | 主要用户任务 | 文档重点 |
| --- | --- | --- | --- |
| 展示 / 总览页 | 以统计卡片、趋势、状态或异常汇总为主 | 确认范围、读取指标、发现异常、进入下钻页面 | 统计口径、更新时间、指标关系、异常判读和下钻路径 |
| 列表查询页 | 以筛选、列表、分页、详情或导出为主 | 设置条件、定位记录、理解空状态、核对明细 | 筛选区、列表字段、查询顺序、空状态和对账方法 |
| 对象管理页 | 围绕一个业务对象执行新增、查看、编辑、删除 | 管理对象完整生命周期 | 列表、详情、表单、确认弹窗、依赖关系和操作后验证 |
| 单页多区域配置页 | 一个页面包含多个配置分区或页签 | 选择配置区域、修改参数、验证影响 | 区域与影响范围映射、通用操作顺序、各区域差异和统一校验 |
| 复合页签 / 多步骤页 | 同一页面同时包含列表页签和一个或多个向导流程 | 在不同任务入口间切换并完成分阶段流程 | 页签任务映射、入口选择、阶段检查点、提交结果和列表状态 |

如果一个页面同时符合多种类型，以用户完成主要任务所需的最长路径作为主类型，其他特征作为补充。

## 4. 各章节的写法

| 章节 | 所有页面都要回答的问题 | 按页面类型调整的重点 |
| --- | --- | --- |
| 功能概述 | 谁使用、从哪里进入、管理什么 | 用一句话说明页面的核心任务，避免“配置、状态和关系”等泛化占位语 |
| 前提条件 | 进入页面或执行操作前需要什么 | 展示页强调数据与权限；操作页强调依赖、审批、影响和恢复条件 |
| 页面说明 | 页面由哪些区域组成，各区域负责什么 | 展示页说明指标关系；列表页说明筛选区和结果区；配置页说明区域与影响范围；复合页说明页签与任务入口 |
| 主要操作 | 用户按什么顺序完成任务 | 展示页用“定范围—读指标—下钻”；列表页用“查询—查看—核对”；对象页用生命周期；配置页先给通用顺序；多步骤页标出阶段检查点 |
| 参数速查表 | 字段是什么、何时需要、如何理解 | 展示页列统计口径；列表页区分查询条件与结果字段；表单页区分表单要求与下游要求 |
| 踩坑提示 | 哪些误解最容易导致错误 | 只写高频误判和页面特有风险，不重复操作步骤 |
| 结果校验 | 完成后能观察到什么 | 展示页验证范围和数据新鲜度；操作页验证页面提示、列表或详情状态及下游影响 |
| 常见问题 | 用户遇到什么症状，如何定位 | 按“现象—可能原因—处理方式”组织，不重复注意事项 |
| 注意事项 | 哪些边界必须持续遵守 | 集中写权限、敏感信息、计费、外部系统和高风险操作边界 |
| 后续操作 | 用户完成本页任务后去哪里 | 指向实际相邻页面或下游验证，不使用笼统的“继续下一步” |

## 5. 内容保留原则

1. 保留已经存在并可由页面、截图或产品资料支持的功能、字段、风险和验证内容。
2. 优先合并重复内容，不因调整结构而删除有价值的操作条件或故障排查信息。
3. 不为追求格式统一而给展示页面虚构保存、提交或配置步骤。
4. 不把未在当前产品中验证的新结构、新功能或新术语写入用户手册。
5. 英文和中文页面的功能范围、操作顺序、参数含义、图片数量和风险边界应对应。

## 6. 图片策略

1. 样本阶段不修改现有图片，只调整图片前后的说明和归属。
2. 每张图片应服务于相邻操作或页面区域，避免与正文脱节。
3. 不用图片替代关键操作步骤；用户在图片无法加载时仍应能完成任务。
4. 图片中的敏感信息按仓库既有规则处理，不在正文中出现“源文档截图”“已脱敏”等面向内部维护的描述。

## 7. 内容边界

- **踩坑提示**：用户容易误判，但尚未发生明确错误。
- **结果校验**：操作或阅读完成后，可直接观察到的成功信号。
- **常见问题**：已经出现具体症状，需要定位原因和处理。
- **注意事项**：无论是否出现问题都必须遵守的长期边界。

相同句子只保留在最适合的章节，避免在每个操作后重复“结果校验、注意事项、常见问题”。

## 8. 五个对比样本

| 页面类型 | 中文页面 | 英文页面 |
| --- | --- | --- |
| 展示 / 总览页 | `ai-infra-on-prem/operator/monitoring/overview/` | `ai-infra-on-prem/operator/monitoring/overview/` |
| 列表查询页 | `ai-infra-on-prem/user/quotas-usage/top-up-records/` | `ai-infra-on-prem/user/quotas-usage/top-up-records/` |
| 对象管理页 | `model-services/operator/settings/model-source/` | `model-services/operator/settings/model-source/` |
| 单页多区域配置页 | `settings/operator/system-settings/platform-settings/` | `settings/operator/system-settings/platform-settings/` |
| 复合页签 / 多步骤页 | `model-services/user/studio/my-models/` | `model-services/user/studio/my-models/` |

## 9. 后续迁移与验收

样本经人工确认后，再按以下顺序扩展：

1. 盘点用户手册页面并标记主类型，不移动目录。
2. 同一类型先处理 3 至 5 个页面，确认表达方式后再扩大范围。
3. 每次同步修改中英文页面，不单独批量改一侧语言。
4. 检查章节完整性、标题对应、图片引用、内部链接和中英文功能一致性。
5. 运行文档配置检查和完整构建，确认无 Markdown、链接或构建错误。

验收时重点比较用户完成任务所需的查找时间、重复段落数量、操作步骤可执行性和结果校验是否可观察，而不是比较每个页面的字数是否一致。
