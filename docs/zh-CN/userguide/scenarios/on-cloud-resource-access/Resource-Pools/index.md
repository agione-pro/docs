---
prev: false
next: true
---

# 接入资源池

本任务从云账号已同步的地域中启用业务允许使用的资源池，并维护便于识别的显示名称。

## 场景目标

只有经过批准的云地域处于启用状态，并能被后续授权流程选择。

## 适用角色

- 平台运营方

## 开始前准备

- 云账号已同步出预期地域。
- 已明确允许使用的地域清单和中英文命名规则。

## 操作步骤

### 启用 / 禁用资源池地域

1. 进入平台首页，点击左侧导航栏的 **"接入资源池"** 菜单，进入资源池管理页面。
2. 在左侧云平台列表上方输入关键字搜索当前版本已支持的云厂商，列表会显示各平台已同步的地域数量；点击目标云平台（如阿里云或 AWS）。华为云接入当前暂不支持。
3. 右侧展示选中云平台的所有地域卡片网格（每行 3 列），地域包括 华北1(青岛)、华北2(北京)、华北3(张家口) 等。
4. 找到需要启用 / 禁用的地域，点击地域卡片右侧的开关按钮：
   - 开关为开启状态时，表示该地域已启用；
   - 点击开关即可切换地域的启用 / 禁用状态。
![接入资源池](./images/resource-pools-list.png)

### 编辑地域显示名称

1. 进入平台首页，点击左侧导航栏的 **"接入资源池"** 菜单，进入资源池管理页面。
2. 在左侧选择目标云厂商（如 阿里云），找到需要编辑的地域。
3. 点击地域卡片上的编辑图标，弹出「编辑名称」窗口。
4. **"显示名称"**（标注"多语言"）：用于设置地域在列表、详情和选择控件中的显示名称。点击 **"English"** / **"中文简体"** 标签切换语言 Tab，**"将在 English 语言环境下显示"** / **"将在中文简体语言环境下显示"** 的提示，分别在对应 Tab 下填写英文与中文简体环境下的名称。
5. 确认配置无误后，点击 **"确定"** 按钮完成修改；如需放弃，点击 **"取消"**。
![编辑名称](./images/edit-name.png)
#### 参数说明

| 字段名称 | 字段类型 | 示例 | 说明 |
|----------|----------|------|------|
| 云厂商 | 选择框 | `阿里云` / `亚马逊` | 必填，选择要管理的云厂商资源池 |
| 地域开关 | 开关 | `开启` / `关闭` | 用于启用或禁用对应地域的资源池 |
| 显示名称 | 多语言文本 | English: `China (Beijing)` / 中文简体: `北京` | 必填，分别在"English"和"中文简体"Tab 下配置显示名称 |

## 完成检查

> **用途：** 以下检查是当前功能任务的退出条件，用于判断操作结果是否可观察、可复核，以及是否可以继续当前场景的下一步。它不是操作步骤的重复；任一项不满足时，请按下方“常见失败分支”继续排查。

| 检查项 | 通过标准 |
| --- | --- |
| 1 | 需要的地域已启用且名称清晰。 |
| 2 | 未批准地域保持禁用。 |
| 3 | 授权页面可以选择已启用地域。 |

## 常见失败分支

| 现象 | 优先检查 |
| --- | --- |
| 列表缺少目标地域 | 云账号同步、云权限和平台地域支持情况 |
| 地域无法授权 | 地域启用状态和资源池保存结果 |

## 操作手册

[查看“接入资源池”的完整字段、校验规则和常见问题](/zh-CN/usermanual/ai-infra-on-cloud/operator/access-management/resource-pools/)
