# 多平台调度快速入门

::: info 文档信息
版本：v1.0
更新日期：2026-07-08
:::

## 30 秒速查

| 我是谁 | 先看什么 | 下一步 |
| --- | --- | --- |
| 首次接触多平台调度 | 先理解云平台、云账号、资源池、授权、部署资产和部署任务的关系。 | 阅读 [从零开始部署云上模型服务](../end-to-end/deploy-cloud-model-service/)。 |
| 运营管理员 | 先完成云平台接入、云账号接入、资源池启用和授权。 | 继续维护模型库、推理框架、推理镜像和调度策略。 |
| 普通用户 | 先确认自己可用的接入账号、云平台、地域和可部署模型。 | 使用 [快速部署](../user/model-services/quick-deployment/) 发起模型部署；如需继续发布，进入 `模型及AI服务 > 创作空间 > 我的部署` 的 `On-Cloud` 列表。 |
| 排障人员 | 先定位是账号、授权、资源池、模型资产、调度策略还是部署任务问题。 | 查看部署详情、API 调用、监控信息和事件记录。 |

## 功能概述

`多平台调度` 汇总 AGIOne 面向多云资源的接入、授权、调度和模型部署能力。它把云平台、云账号、资源池、业务授权、模型库、推理框架、推理镜像和调度策略串成一条完整链路：运营管理员先接入和治理多云资源，普通用户再基于已开放的云资源快速部署模型服务，并在部署后查看 API、监控和事件。

| 项目 | 内容 |
| --- | --- |
| 适用角色 | 运营管理员、普通用户 |
| 推荐入口 | 本文档、[多云调度平台概览](../)、[从零开始部署云上模型服务](../end-to-end/deploy-cloud-model-service/) |
| 关键对象 | 云平台、云账号、资源池、租户授权、业务授权、模型库、推理框架、推理镜像、调度策略、部署任务 |
| 典型用途 | 建立多平台调度资源理解框架，明确运营方与用户的操作边界，并选择正确阅读路径 |

#### 新手理解

多平台调度像多云资源接入手册：先把云账号、资源池和授权关系理顺，再让用户基于被授权资源部署模型服务。

## 适用角色

1. 已明确当前账号属于运营管理员还是普通用户。
2. 运营管理员应准备云账号授权、资源池规划、部署资产和调度策略。
3. 普通用户应确认自己具备接入账号、地域资源和模型服务部署权限。
4. 所有截图、示例和工单中不得暴露 AK/SK、API Key、Endpoint、账号 ID 或内部资源编号。

## AGIOne / 多平台调度是什么

AGIOne 面向模型服务、算力资源和运营治理提供一体化平台能力。多平台调度指平台纳管公有云或第三方云资源后，为模型部署提供跨云资源选择、智能推荐、部署任务管理和调用观测能力的场景。

在多平台调度场景中，平台负责：

1. 记录可接入的云平台和资源边界，例如云厂商、地域和资源池。
2. 维护可部署能力，例如云账号、推理框架、推理镜像、模型库和调度策略。
3. 管理用户侧部署入口，例如快速部署、我的部署、API 调用和监控事件。
4. 通过授权关系控制租户、业务和用户能使用哪些云平台与资源池。

## 角色关系

| 角色 | 主要职责 | 常用章节 |
| --- | --- | --- |
| 运营管理员 | 接入云平台、云账号和资源池；配置租户与业务授权；维护模型库、框架、镜像和调度策略。 | [快速接入](../operator/access-workbench/quick-start/)、[接入云平台](../operator/access-management/cloud-platforms/)、[模型库](../operator/deploy-assets/models/) |
| 普通用户 | 添加或使用已授权云账号；从模型库选择模型；基于智能推荐发起部署；查看部署详情、API、监控和事件。 | [接入账号](../user/access-management/access-accounts/)、[快速部署](../user/model-services/quick-deployment/)、[我的部署](../user/model-services/my-deployments/) |

运营管理员决定“哪些云资源和模型资产可以部署”，普通用户决定“把哪个模型部署到哪个已授权云资源上”。如果用户侧看不到某个模型、云账号或地域，通常需要从账号接入、租户授权、业务授权、资源池状态和部署资产几个方向排查。

## 资源与授权层级

| 层级 | 说明 | 影响范围 |
| --- | --- | --- |
| 云平台 | 可接入的云厂商或云服务类型。 | 影响云账号、资源池和模型部署点的归属。 |
| 云账号 | 访问目标云平台资源的账号凭据或账号实体。 | 影响资源同步、模型部署和费用归属。 |
| 资源池 | 云平台下可启用的地域或资源集合。 | 影响用户可部署的地域、规格和库存范围。 |
| 租户-云平台授权 | 控制租户是否可以使用目标云平台。 | 影响租户下用户能否看到或使用某云平台。 |
| 业务-资源池授权 | 控制业务或用户范围能使用哪些资源池。 | 影响快速部署中的可部署范围。 |
| 部署资产 | 模型库、推理框架和推理镜像等可部署基础数据。 | 影响模型是否能被选择、推荐和发起部署。 |
| 部署任务 | 用户提交的云上模型服务部署实例。 | 产生状态、API 调用、监控和事件数据。 |

推荐配置顺序是：先接入云平台，再添加云账号和启用资源池，然后配置租户与业务授权，之后维护模型库、框架、镜像和策略，最后开放快速部署给用户。

## 用户侧与运营方侧功能边界

| 能力 | 运营管理员 | 普通用户 |
| --- | --- | --- |
| 云平台 | 新增、编辑、启用、禁用云平台类型。 | 选择已授权云平台作为部署范围。 |
| 云账号 | 接入和管理运营侧云账号。 | 添加或使用个人可用的接入账号。 |
| 资源池 | 启用、禁用资源池地域，维护显示名称。 | 在快速部署中选择已授权资源池。 |
| 授权 | 配置租户-云平台授权和业务-资源池授权。 | 通过可见范围感知授权结果。 |
| 部署资产 | 维护模型库、推理框架和推理镜像。 | 从可部署模型中选择目标模型。 |
| 调度策略 | 维护策略管理和候选排序规则。 | 在快速部署中选择业务策略并查看推荐结果。 |
| 部署任务 | 查看接入与资源治理结果。 | 发起部署、启停部署、发布部署、查看 API、监控和事件。 |

## 全局核心术语

| 术语 | 说明 |
| --- | --- |
| 云平台 | 被 AGIOne 接入的云厂商或云资源平台。 |
| 接入账号 | 用于访问云平台资源的账号配置，可能包含 AK/SK 等敏感凭据。 |
| 资源池 | 云平台下可用于部署的地域、算力、规格和库存能力集合。 |
| 租户授权 | 将云平台能力开放给指定租户的授权关系。 |
| 业务授权 | 将资源池能力开放给指定业务或用户范围的授权关系。 |
| 模型库 | 运营方维护的可部署模型资产集合。 |
| 推理框架 | 模型运行所需的推理引擎或服务框架，例如 vLLM、SGLang。 |
| 推理镜像 | 部署模型服务时使用的运行时镜像。 |
| 业务策略 | 快速部署中影响候选资源排序的策略，例如经济适用、卓越性能或现货速达。 |
| 部署任务 | 用户发起的一次云上模型部署记录。 |

## 前提条件

1. 已确认当前账号是运营管理员还是普通用户。
2. 运营管理员已准备云账号授权、资源池规划和最小权限策略。
3. 普通用户已确认自己有目标云账号、地域和部署权限。

## 参数说明

| 字段名称 | 是否必填 | 字段类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| 角色类型 | 是 | 枚举 | 普通用户 | 用于判断阅读运营侧接入配置还是用户侧部署流程。 |
| 云账号 | 否 | 文本 | 示例云账号 A | 用于定位接入账号、云账号授权和资源同步状态。 |
| 资源池 | 否 | 文本 | 示例资源池 | 用于确认可部署地域、规格和库存范围。 |
| 部署资产 | 否 | 文本 | 示例模型 / 示例镜像 | 用于核对模型库、推理框架和推理镜像是否匹配。 |

## 结果校验

- 能说清云账号、资源池、租户授权和业务地域授权的关系。
- 知道快速部署依赖模型、框架、镜像、授权和调度策略。
- 知道部署失败时应查看我的部署事件、资源池状态和授权范围。
- 知道快速部署提交后，可在 `模型及AI服务 > 创作空间 > 我的部署` 的 `On-Cloud` 列表选择发布区域，并跳转到 [我的模型](../../model-services/user/studio/my-models/) 的发布模型页面。

## 踩坑提示

- 不要只接入云平台类型就开放给用户，云账号校验、资源池同步和授权配置缺一不可。
- 用户看不到地域或规格时，优先检查租户授权、业务资源池授权和资源池启用状态。
- 模型、框架、镜像和调度策略要同时匹配，单独维护模型库不能保证部署成功。
- 部署失败时先保留脱敏部署 ID、地域、模型、规格和事件摘要，再进入运营侧排查。
- 入门学习只用于理解云账号、资源池、授权、部署资产、调度策略和部署任务关系，不用于执行真实变更。
- 不执行真实 `快速部署`、`发起部署`、启停部署、发布部署、`删除`、`禁用`、`解绑` 或授权调整。
- 不写真实 AK/SK、API Key、Token、Endpoint、账号 ID、部署 ID、资源池 ID、客户信息、费用明细或测试参数。

## 推荐阅读路径

#### 运营管理员

1. 阅读本文，确认角色边界和资源授权层级。
2. 完成 [快速接入](../operator/access-workbench/quick-start/)。
3. 维护 [接入云平台](../operator/access-management/cloud-platforms/) 和 [接入账号](../operator/access-management/cloud-accounts/)。
4. 启用 [接入资源池](../operator/access-management/resource-pools/)。
5. 配置 [租户-云平台授权](../operator/auth-management/tenant-cloud-auth/) 和 [业务-资源池授权](../operator/auth-management/business-region-auth/)。
6. 维护 [模型库](../operator/deploy-assets/models/)、[推理框架](../operator/deploy-assets/frameworks/) 和 [推理镜像](../operator/deploy-assets/runtime-images/)。
7. 配置 [策略管理](../operator/scheduling-governance/policies/)。

#### 普通用户

1. 阅读本文，确认自己需要的云平台、云账号和模型范围。
2. 在 [接入账号](../user/access-management/access-accounts/) 添加或确认云账号。
3. 使用 [快速部署](../user/model-services/quick-deployment/) 选择云账号、地域和模型。
4. 根据推理引擎、业务策略和推荐方案发起部署。
5. 在 [我的部署](../user/model-services/my-deployments/) 查看任务状态、API 调用、监控信息和事件记录。
6. 如需继续发布模型，进入 `模型及AI服务 > 创作空间 > 我的部署` 的 `On-Cloud` 列表，选择发布区域后跳转到 [我的模型](../../model-services/user/studio/my-models/) 的发布模型页面。

## 常见问题

#### 快速部署看不到目标云账号或地域

**问题现象：**

用户进入快速部署页面后，目标云账号、云平台或地域不在可部署范围中。

**可能原因：**

- 用户尚未添加接入账号，或账号状态异常。
- 运营管理员未启用对应云平台或资源池。
- 租户-云平台授权或业务-资源池授权未配置。
- 页面筛选条件限制了云平台、地域或模型范围。

**处理方式：**

1. 用户先检查 [接入账号](../user/access-management/access-accounts/) 是否存在且可用。
2. 请运营管理员检查云平台、云账号和资源池状态。
3. 检查租户授权和业务授权是否覆盖当前用户或业务。
4. 清空快速部署筛选条件后刷新资源。

#### 部署任务提交后长时间无结果

**问题现象：**

部署任务提交后长期处于创建中、部署中或无可用 API 状态。

**可能原因：**

- 目标云资源库存不足或规格不可用。
- 推理框架、镜像或模型部署点配置不完整。
- 云账号权限不足或云平台接口调用失败。
- 调度策略筛选范围过窄。

**处理方式：**

1. 在 [我的部署](../user/model-services/my-deployments/) 查看部署详情、事件记录和监控信息。
2. 尝试切换推理引擎、业务策略或地域后重新部署。
3. 请运营管理员检查模型库、推理框架、推理镜像和资源池配置。
4. 检查云账号权限和云平台侧资源状态。

## 后续操作

1. 运营管理员继续阅读接入云平台、云账号、资源池、授权管理和部署资产页面。
2. 普通用户继续阅读接入账号、快速部署和我的部署页面；如需发布模型，继续查看 [模型及AI服务的我的部署](../../model-services/user/studio/my-deployments/) 和 [我的模型](../../model-services/user/studio/my-models/)。
3. 需要完整链路时，继续阅读 [从零开始部署云上模型服务](../end-to-end/deploy-cloud-model-service/)。

## 注意事项

- 本文用于平台级入门，不替代各功能页的具体字段说明。
- 涉及云账号、AK/SK、API Key、Token、Endpoint、账号 ID、部署 ID、资源池 ID、客户信息、费用明细、私钥和证书的操作，应使用平台密钥管理能力保存，不要写入文档或截图。
- 真实快速部署、发起部署、启停部署、发布部署、删除、禁用、解绑和授权调整可能影响用户任务，应先确认影响范围和回退方案。
