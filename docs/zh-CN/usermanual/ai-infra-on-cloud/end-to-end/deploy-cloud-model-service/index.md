# 从零开始部署云上模型服务

::: info 文档信息
版本：v1.0
更新日期：2026-07-08
:::

## 功能概述

本文把运营管理员和普通用户的多平台调度操作串成一条端到端路径：运营管理员先接入云平台、云账号、资源池、授权关系、模型资产和调度策略，普通用户再选择云账号与模型，通过智能推荐发起部署，并在部署后查看 API 调用、监控和事件记录。

| 项目 | 内容 |
| --- | --- |
| 适用角色 | 运营管理员、普通用户 |
| 推荐前置阅读 | [多平台调度快速入门](../../getting-started/)、[快速接入](../../operator/access-workbench/quick-start/)、[快速部署](../../user/model-services/quick-deployment/) |
| 输出结果 | 一个可查看状态、API、监控和事件的云上模型服务部署任务 |
| 典型用途 | 新云平台首次联调、模型资产上架验证、部署流程演示、上线前验收和问题定位 |

#### 新手理解

从零部署云上模型服务像安排一次云资源接力：运营管理员准备云账号、资源池、授权、模型和调度策略，普通用户选择资源并发起部署。

## 端到端流程图

| 阶段 | 操作方 | 目标 |
| --- | --- | --- |
| 云平台接入 | 运营管理员 | 创建或启用可接入云平台。 |
| 云账号接入 | 运营管理员 | 添加云账号，确保平台可同步云资源。 |
| 资源池准备 | 运营管理员 | 启用地域资源池并确认可用范围。 |
| 授权开放 | 运营管理员 | 配置租户-云平台授权和业务-资源池授权。 |
| 部署资产准备 | 运营管理员 | 维护模型库、推理框架、推理镜像和部署配置。 |
| 策略治理 | 运营管理员 | 配置影响推荐排序的业务策略。 |
| 快速部署 | 普通用户 | 选择云账号、地域、模型、推理引擎和业务策略，发起部署。 |
| 状态验证 | 普通用户 | 查看部署状态、API 调用、监控信息和事件记录。 |
| 排障闭环 | 双方 | 根据失败路径检查账号、授权、资源池、模型资产和策略。 |

#### 术语速查

| 术语 | 说明 |
| --- | --- |
| 云账号校验 | 验证云账号密钥、权限和资源同步是否可用。 |
| 资源池启用 | 把云账号内资源纳入可授权和可调度范围。 |
| 业务地域授权 | 控制具体业务能部署到哪些地域。 |
| 我的部署 | 普通用户查看部署状态、Endpoint、事件和监控的入口。 |

## 前提条件

1. 运营管理员具备云平台接入、云账号、资源池、授权、部署资产和策略管理权限。
2. 普通用户具备接入账号、快速部署和我的部署的菜单权限。
3. 目标云平台账号具备创建或管理部署所需的云资源权限。
4. 租户和业务已获得目标云平台与资源池授权。
5. 目标模型、推理框架、推理镜像和部署配置已准备。
6. 本次验证使用的账号和凭据已脱敏记录，不在文档中保存真实 AK/SK。

## 参数说明

| 字段名称 | 是否必填 | 字段类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| 云平台类型 | 是 | 枚举 | 示例云平台 | 用于判断账号接入、资产同步和部署能力来源。 |
| 云账号 | 是 | 文本 | CLOUD-ACCOUNT-001 | 用于完成资源同步、授权和部署提交，文档中只写占位符。 |
| 资源池 | 是 | 文本 | 示例资源池 A | 用户部署时实际调度的云上资源范围。 |
| 业务地域 | 是 | 枚举 | 华东示例区 | 控制用户侧可见地域和部署入口。 |
| 模型资产 | 是 | 文本 | 示例模型 A | 快速部署时选择的模型、框架和镜像组合。 |
| 部署任务 | 系统生成 | 文本 | DEPLOY-202607130001 | 用于在我的部署中追踪状态、事件和调用信息。 |

## 踩坑提示

- 云账号校验通过不代表用户能部署，还要完成资源池同步、租户授权和业务地域授权。
- 快速部署没有可选规格时，不要只改模型参数，应先检查资源池库存和调度策略。
- 部署创建中或失败时，先看我的部署事件记录，再决定是否回到资源池、镜像或云账号页面排查。
- `确认部署`、`发起部署`、`发布`、`提交`、`保存`、`删除`、启停部署和授权调整均属于高风险动作，执行前必须确认影响范围和回退方案。
- 学习或截图场景只查看部署推荐、弹窗字段、任务状态、事件、监控和跳转，不提交真实部署。
- 文档和工单中只记录脱敏后的账号、资源池、部署 ID 和错误摘要，不写真实云账号、AK/SK、API Key、Token、Endpoint、账号 ID、费用明细、客户信息或内部错误日志。

## 步骤 1：运营管理员接入云平台

1. 进入 `接入管理 > 接入云平台`。
2. 点击 `添加平台`，填写云平台名称、平台类型、标识和描述。
3. 提交后确认云平台出现在列表中，并处于可使用状态。
4. 如需按向导操作，可先进入 [快速接入](../../operator/access-workbench/quick-start/) 查看整体接入进度。

**结果校验：**

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 云平台列表中能看到目标云平台 | 云平台列表中能看到目标云平台。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 云平台状态、名称和标识符合预 | 云平台状态、名称和标识符合预期。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 后续云账号和资源池配置中可选 | 后续云账号和资源池配置中可选择该云平台。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |

## 步骤 2：运营管理员接入云账号

1. 进入 `接入管理 > 接入账号`。
2. 选择目标云平台，点击 `添加云账号`。
3. 填写账号名称、账号标识、AK/SK 或平台要求的认证信息。
4. 提交后确认账号出现在列表中，并能用于资源同步或部署。

**结果校验：**

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 目标云账号在 接入账号 列表 | 目标云账号在 [接入账号](../../operator/access-management/cloud-accounts/) 列表中可见。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 账号状态正常 | 账号状态正常，且归属云平台正确。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 账号凭据未暴露在文档、截图或 | 账号凭据未暴露在文档、截图或工单中。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |

## 步骤 3：运营管理员启用资源池

1. 进入 `接入管理 > 接入资源池`。
2. 选择目标云平台，查看地域卡片网格。
3. 启用目标地域资源池，必要时编辑地域显示名称。
4. 确认目标资源池可用于后续授权和部署。

**结果校验：**

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 接入资源池 中目标地域处于启 | [接入资源池](../../operator/access-management/resource-pools/) 中目标地域处于启用状态。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 地域名称、云平台归属和资源池 | 地域名称、云平台归属和资源池信息准确。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 普通用户部署时的目标地域与资 | 普通用户部署时的目标地域与资源池配置一致。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |

## 步骤 4：运营管理员配置授权

1. 进入 `授权管理 > 租户-云平台授权`，为目标租户新增云平台授权。
2. 进入 `授权管理 > 业务-资源池授权`，为目标业务或用户范围授权目标资源池地域。
3. 确认授权范围覆盖本次验证使用的租户、业务、云平台和资源池。

**结果校验：**

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 租户-云平台授权 中存在目标 | [租户-云平台授权](../../operator/auth-management/tenant-cloud-auth/) 中存在目标授权记录。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 业务-资源池授权 中存在目标 | [业务-资源池授权](../../operator/auth-management/business-region-auth/) 中存在目标资源池授权记录。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 普通用户登录后能看到对应云平 | 普通用户登录后能看到对应云平台和地域范围。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |

## 步骤 5：运营管理员准备部署资产

1. 进入 `部署资产 > 模型库`，添加或确认目标模型。
2. 在模型库中配置元模型、上架版本、云上部署点、输出配置和计算方案。
3. 进入 `部署资产 > 推理框架`，确认目标推理框架可用。
4. 进入 `部署资产 > 推理镜像`，确认目标运行镜像可用。

**结果校验：**

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 模型库 中目标模型可见 | [模型库](../../operator/deploy-assets/models/) 中目标模型可见。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 模型关联的云平台、地域、推理 | 模型关联的云平台、地域、推理框架、镜像和计算方案完整。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 普通用户快速部署页能筛选到目 | 普通用户快速部署页能筛选到目标模型。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |

## 步骤 6：运营管理员配置调度策略

1. 进入 `调度治理 > 策略管理`。
2. 新增或确认用于部署推荐的业务策略。
3. 检查策略名称、适用范围、排序口径和状态。

**结果校验：**

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 策略管理 中目标策略可见 | [策略管理](../../operator/scheduling-governance/policies/) 中目标策略可见。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 快速部署中的业务策略选项与策 | 快速部署中的业务策略选项与策略配置一致。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 策略不会把可用资源全部排除 | 策略不会把可用资源全部排除。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |

## 步骤 7：普通用户添加或确认接入账号

1. 进入 `访问管理 > 接入管理`。
2. 点击 `添加云账号` 或确认已有云账号可用。
3. 填写账号名称、云平台、认证信息和必要说明。
4. 返回列表确认账号可见。

**结果校验：**

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 接入账号 中目标账号可见 | [接入账号](../../user/access-management/access-accounts/) 中目标账号可见。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 账号云平台与运营管理员授权的 | 账号云平台与运营管理员授权的云平台一致。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 账号凭据已脱敏保存 | 账号凭据已脱敏保存。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |

## 步骤 8：普通用户发起快速部署

1. 进入 `模型服务 > 快速部署`。
2. 在模型库页选择目标云平台、地域和模型。
3. 点击目标模型的 `部署模型`。
4. 在智能推荐部署页选择部署模式、推理引擎和业务策略。
5. 确认模型算力匹配方案；点击最终 `确认部署` 前再次核对模型、规格、地域、费用和资源影响。
6. 在发布部署弹窗中选择云账号，填写任务名称和任务描述。
7. 真实部署场景点击 `发起部署` 提交部署任务；学习或截图场景只查看弹窗字段，不点击最终按钮。

**结果校验：**

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 快速部署 能看到目标云账号、 | [快速部署](../../user/model-services/quick-deployment/) 能看到目标云账号、地域和模型。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 智能推荐页能生成至少一个可部 | 智能推荐页能生成至少一个可部署方案。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 发布部署弹窗中必填项填写后可 | 发布部署弹窗中必填项填写后可提交。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |

## 步骤 9：普通用户验证部署状态

1. 进入 `AI Infra > On-Cloud > 模型服务 > 我的部署`。
2. 在部署任务列表中查找刚提交的快速部署任务，确认任务状态、模型、地域和资源规格。
3. 打开详情页，查看基本信息、API 调用、监控信息和事件记录。
4. 同时进入 `模型及AI服务 > 创作空间 > 我的部署` 的 `On-Cloud` 列表，确认同一部署记录可见。
5. 如需继续发布模型，在 `On-Cloud` 列表选择目标部署并选择发布区域。
6. 选择发布区域后，页面跳转到 `模型及AI服务 > 创作空间 > 我的模型` 的发布模型页面；学习或截图时不要执行最终 `发布`、`提交` 或 `保存`。

**结果校验：**

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| AI Infra 我的部署中能看到目标任务 | [我的部署](../../user/model-services/my-deployments/) 中能看到目标快速部署任务。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 任务状态符合预期 | 任务状态符合预期，失败时能看到事件记录。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| API 调用信息、监控信息和 | API 调用信息、监控信息和基本信息可查看。 | 未达到时回到本步骤核对前置条件、权限和配置状态 |
| 模型及AI服务我的部署记录可见 | `模型及AI服务 > 创作空间 > 我的部署` 的 `On-Cloud` 列表中能看到同一部署记录。 | 未达到时检查模型服务侧权限、部署筛选条件和任务同步状态 |
| 发布页面跳转正确 | 选择发布区域后，页面跳转到 `模型及AI服务 > 创作空间 > 我的模型` 的发布模型页面。 | 未达到时检查发布入口、发布区域和目标部署状态 |

## 失败分支与排查路径

#### 失败分支：云账号校验失败

下一跳：[云账号](../../operator/access-management/cloud-accounts/)

**问题现象：**

运营管理员添加云账号后校验不通过或无法同步资源。

**可能原因：**

AK/SK、授权策略、账号状态或云厂商 API 权限不满足资源查询和部署要求。

**处理方式：**

1. 检查 AK/SK、授权策略、账号状态和云厂商 API 权限。
2. 确认密钥未过期，且最小权限覆盖资源查询和部署所需操作。
3. 校验通过后再启用资源池和配置租户授权。

#### 失败分支：资源池不可用

下一跳：[资源池](../../operator/access-management/resource-pools/)

**问题现象：**

用户已获授权，但快速部署页没有可用地域、规格或资源池。

**可能原因：**

资源池未启用、容量未同步、地域未授权，或租户授权和业务地域授权没有同时覆盖当前用户。

**处理方式：**

1. 检查资源池是否启用、容量是否同步、地域是否被授权。
2. 核对租户授权和业务地域授权是否同时覆盖当前用户。
3. 确认模型部署资产和调度策略没有限制到其他资源池。

#### 失败分支：部署创建失败

下一跳：[我的部署](../../user/model-services/my-deployments/)

**问题现象：**

快速部署提交后失败，或我的部署中长时间创建中。

**可能原因：**

资源不足、镜像拉取失败、推理框架异常、云账号认证失败，或模型、规格和调度策略不匹配。

**处理方式：**

1. 查看我的部署事件，区分资源不足、镜像失败、框架异常或认证错误。
2. 核对模型、推理框架、运行镜像、规格和调度策略是否匹配。
3. 将脱敏部署 ID、地域、模型名和错误摘要交给运营管理员复核。

## 结果校验

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 资源可见 | 快速部署页能选择目标模型、地域、账号和规格 | 继续提交云上部署 |
| 状态正常 | 我的部署显示运行中或可用，并生成调用信息 | 进入 API 访问或监控 |
| 排障入口可用 | 事件、状态和监控能定位部署错误 | 按失败分支继续排查 |

## 常见问题

#### 云账号已经接入，为什么快速部署仍不可用？

**问题现象：**

普通用户进入快速部署时看不到目标云账号、资源池或模型服务规格。

**可能原因：**

云账号状态异常；资源池未启用；租户或业务区域授权未覆盖当前用户；部署资产或调度策略尚未准备完成。

**处理方式：**

先回到云账号和资源池页面确认状态；再核对租户-云平台授权、业务-资源池授权和调度策略；最后让用户刷新快速部署页面重新选择。

#### 部署创建成功但服务不可用怎么办？

**问题现象：**

部署记录已生成，但状态长时间停留在创建中、异常或无法访问。

**可能原因：**

运行镜像、模型资产、资源池容量、网络访问或调度策略不满足要求。

**处理方式：**

进入我的部署查看状态和错误信息；核对运行镜像、模型库、资源池容量和调度策略；必要时回到部署资产和资源池页面继续排查。

#### 金额、资源或权限信息对不上怎么办？

**问题现象：**

部署页面、授权页面和资源池页面展示的可用范围不一致。

**可能原因：**

当前账号、租户、业务区域、云平台或资源池范围不一致，或页面数据存在同步延迟。

**处理方式：**

统一账号、租户、业务区域和资源池范围后重新查看；保留脱敏后的页面路径、时间和资源池名称，再联系平台运营确认。


## 后续操作

1. 普通用户进入我的部署，持续查看服务状态、Endpoint、事件和监控。
2. 如需发布模型，进入 `模型及AI服务 > 创作空间 > 我的部署` 的 `On-Cloud` 列表，选择发布区域后跳转到 [我的模型](../../../model-services/user/studio/my-models/) 的发布模型页面。
3. 使用脱敏调用示例验证模型输出、延迟和错误码。
4. 运营管理员定期复核云账号授权、资源池容量、调度策略和部署资产版本。
5. 发现费用或容量异常时，结合多平台调度用量、模型及AI服务调用记录和云厂商账单交叉核对。
