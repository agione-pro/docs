# 月度计量

::: info 文档信息
版本：v1.0
更新日期：2026-07-08
:::

## 功能概述

`月度计量` 用于按账期查看月度汇总计量结果。

| 项目 | 内容 |
| --- | --- |
| 适用角色 | 运营方 |
| 导航路径 | AI基础设施 > On-Prem > 配额&计量 > 月度计量 |
| 页面路由 | `/powerone/quota-metric/month` |
| 管理对象 | 账期、状态、创建时间和月度汇总记录 |
| 典型途径 | 月末核对、账期确认、发现月度消耗异常 |

#### 新手理解

月度用量像每月资源账单汇总，把分散的算力、存储和实例消费汇总到月份、租户和资源类型维度。

#### 查看流程

1. 进入 `配额&计量 > 月度计量`。
2. 按时间、状态、资源类型或关键字筛选。
3. 查看列表或图表结果。
4. 如发现异常，进入关联页面继续下钻。

#### 术语速查

| 术语 | 说明 |
| --- | --- |
| 账期 | 计量归属月份或周期。 |
| 月度汇总 | 按账期汇总后的消耗结果。 |

## 前提条件

1. 当前账号具备运营方权限。
2. 目标地域已选择正确。
3. 相关资源、作业或计量任务已有数据上报。

## 页面说明

月度计量用于按月份汇总租户资源消耗、Credits 折算和导出状态。运营方可以先看月度汇总，再下钻到计量明细核对异常增长、跨周期资源或延迟入账。

下图展示月度计量页面。

![月度计量页面](./images/monthly-usage-list.png)

## 主要操作

### 查看月度计量

#### 操作步骤

1. 进入 `AI基础设施 > On-Prem > 配额&计量 > 月度计量`。
2. 按账期或状态筛选。
3. 查看列表中的账期状态和创建时间。
4. 如月度汇总异常，进入计量明细核对。

## 参数说明

| 字段名称 | 是否必填 | 字段类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| 月份 | 必填 | 月份选择 | `2026-07` | 月度统计所属月份。 |
| 租户 | 条件必填 | 下拉选择 | `tenant-a` | 查看指定租户的月度用量。 |
| 资源类型 | 条件必填 | 枚举 | `GPU` | 按算力、存储、实例等类别汇总。 |
| 汇总用量 | 系统生成 | 数字 / 容量 | `960 卡时` | 该月份累计使用量。 |
| 费用 / Credits | 系统生成 | 数字 | `28800` | 按计量规则折算后的费用或 Credits。 |
| 导出状态 | 系统生成 | 状态 | `已生成` | 月度报表是否可下载或仍在生成中。 |

## 踩坑提示

- 月度用量是汇总口径，不能只凭月总量判断具体实例是否计量正确。
- 月末或补采期间数据可能变化，对账前确认账期是否已冻结或完成统计。
- 租户、资源类型和计费单位不一致时，月度汇总和计量明细会出现差异。
## 结果校验

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 账期列表符合预期 | 账期列表符合预期。 | 未达到时检查租户、账期、额度、用量记录和计量同步状态 |
| 汇总与明细合计可以对应 | 汇总与明细合计可以对应。 | 未达到时检查租户、账期、额度、用量记录和计量同步状态 |


## 配置规则与影响

- **先汇总后明细**：月度异常应回到计量明细追溯。
- **账期不可混用**：核对时明确账期范围。

## 常见问题

#### 月度计量数据没有更新

**问题现象：**

进入月度计量页面后，本月数据为空或仍停留在旧时间。

**可能原因：**

- 计量任务尚未执行完成。
- 筛选月份或租户不正确。
- 目标租户没有产生可计量资源。

**处理方式：**

1. 确认筛选月份和租户。
2. 查看计量任务执行时间。
3. 进入计量明细核对是否有实例消费记录。

#### 月度汇总与计量明细不一致

**问题现象：**

月度计量总量和计量明细累计值对不上。

**可能原因：**

- 明细与汇总刷新时间不同。
- 筛选维度、租户或规格不一致。
- 存在跨月运行实例或补算记录。

**处理方式：**

1. 统一筛选条件后重新核对。
2. 查看跨月实例和补算记录。
3. 以计量任务完成后的汇总作为结算依据。

## 后续操作

1. 月度汇总异常时，下钻到计量明细核对资源和时间范围。
2. 结算前确认统计周期、延迟入账和修正记录是否处理完毕。
3. 导出报表后，按租户或业务线进行内部复核。
4. 发现费用异常增长时，结合监控和作业记录定位高消耗资源。

## 注意事项

- 月度用量用于经营和结算参考，发布前应确认统计口径和最终入账状态。
- 导出报表可能包含租户名称、费用和用量明细，应限制分发范围。
- 跨月运行的资源可能按周期拆分，不要只用单日明细推断整月费用。
